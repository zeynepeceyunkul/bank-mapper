// Dev build (ng serve / ng build development) burayı kullanır. Production
// build environment.prod.ts'i kullanır (bkz. angular.json fileReplacements).
export const environment = {
  apiUrl: 'http://localhost:5299/api',
};
